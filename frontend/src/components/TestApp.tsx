import React from 'react'
import MainLayout from './MainLayout'

// Simple test wrapper to ensure our app works
export const TestApp: React.FC = () => {
  return (
    <div>
      <MainLayout />
    </div>
  )
}

export default TestApp